@model IEnumerable<HiNSimulator2014.Models.ValidCommandsForThings>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Command.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Thing.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Command.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Thing.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ValidCommandsForThingsID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ValidCommandsForThingsID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ValidCommandsForThingsID })
        </td>
    </tr>
}

</table>
