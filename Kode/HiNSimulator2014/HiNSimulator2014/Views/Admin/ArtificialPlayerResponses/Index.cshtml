@model IEnumerable<HiNSimulator2014.Models.ArtificialPlayerResponse>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ArtificialPlayer.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ResponseText)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ArtificialPlayer.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ResponseText)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ArtificialPlayerResponseID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ArtificialPlayerResponseID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ArtificialPlayerResponseID })
        </td>
    </tr>
}

</table>
